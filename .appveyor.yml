# ~*~ auto-generated by haskell-ci with config : acd425cd61984b281e076f791e485a5cf28891a29d476fce3b6faa7e08deaeed ~*~

version: "{build}"
clone_folder: C:\project
build: off
cache:
  - "C:\\SR -> .appveyor.yml"

environment:
  global:
    STACK_ROOT: "C:\\SR"
  matrix:
    - { BUILD: "ghc-8.8", STACKCMD: "stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps", STACKCFG: "{ resolver: nightly-2019-09-29, packages: [ encoding, parse, types ], extra-deps: [], flags: {} }", STACKURL: "https://www.stackage.org/stack/windows-x86_64" }

matrix:
  fast_finish: true

install:
  - set PATH=C:\Program Files\Git\mingw64\bin;%PATH%
  - curl -ostack.zip -L %STACKURL%
  - 7z x stack.zip stack.exe
  - refreshenv
test_script:
  - echo %STACKCFG% > stack.yaml
  - stack setup > nul
  - echo "" | %STACKCMD%


